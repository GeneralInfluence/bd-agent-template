{
  "$schema": "https://agents.pinata.cloud/schemas/manifest.v1.json",
  "version": 1,
  "agent": {
    "name": "BD Agent",
    "description": "A BD pipeline agent that handles warm introductions via Telegram, qualifies leads through natural conversation, tracks opportunities in your Supabase database, and notifies your BD steward when something is ready to move.",
    "vibe": "Ushering people through your BD funnel — from warm intro to funded deal — with a focus on accurate status and clear next steps.",
    "emoji": "🌀"
  },
  "template": {
    "slug": "bd-agent",
    "category": "productivity",
    "partnerName": "RaidGuild",
    "tags": ["bd", "crm", "telegram", "pipeline", "leads", "web3"]
  },
  "channels": {
    "telegram": {
      "token_secret": "TELEGRAM_BOT_TOKEN"
    }
  },
  "secrets": [
    {
      "name": "TELEGRAM_BOT_TOKEN",
      "description": "Your Telegram bot token from @BotFather. Must have Group Privacy turned OFF.",
      "required": true
    },
    {
      "name": "SUPABASE_URL",
      "description": "Your Supabase project URL (e.g. https://xxxxx.supabase.co)",
      "required": true
    },
    {
      "name": "SUPABASE_ANON_KEY",
      "description": "Your Supabase anon/public API key",
      "required": true
    },
    {
      "name": "ANTHROPIC_API_KEY",
      "description": "Anthropic API key for qualifying conversations. Only invoked on @mentions — not every message.",
      "required": true
    },
    {
      "name": "ORG_NAME",
      "description": "Your organization's name — used in bot greetings and context.",
      "required": true
    },
    {
      "name": "BD_STEWARD_TELEGRAM_ID",
      "description": "Telegram user ID of the person who receives confirmed lead notifications. They must DM the bot once first.",
      "required": false
    },
    {
      "name": "PRISM_API_KEY",
      "description": "(Optional) PrismBOT API key. Gives the agent access to your community's meeting memory, decisions, and knowledge base.",
      "required": false
    },
    {
      "name": "PRISM_API_URL",
      "description": "(Optional) PrismBOT API base URL.",
      "required": false
    },
    {
      "name": "DITTO_API_KEY",
      "description": "(Optional) Ditto knowledge graph API key. Enables contribution attribution for rewards/kudos calculation when deals close. Get a key at heyditto.ai.",
      "required": false
    },
    {
      "name": "STEWARD_TZ",
      "description": "(Optional) IANA timezone for the BD steward (e.g. America/Los_Angeles). Used for daily stale-lead checks and weekly pipeline summaries. Defaults to America/Los_Angeles.",
      "required": false
    }
  ],
  "scripts": {
    "build": "cd workspace/bot && npm install",
    "start": "cd workspace/bot && pm2 start bot.js --name bd-agent-bot --no-daemon || node bot.js"
  },
  "tasks": []
}

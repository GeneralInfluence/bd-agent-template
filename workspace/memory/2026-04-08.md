# 2026-04-08

## BD Bot Architecture Shift
- Sean wants OpenClaw to run the Telegram bot directly — no separate byzved server
- Received byzved source code (zip via Telegram), unpacked to `workspace/byzved/`
- byzved = standalone Grammy.js bot → Supabase + embeddings. We're replacing it.
- All CRM data goes through **Guild Grimoire Ingest API**, not direct Supabase
- Created `skills/raidguild-bd/` skill with Grimoire API wrapper
- Credentials stored in `.env.raidguild`
- API endpoint: `POST https://guild-grimoire.xyz/api/modules/guild-grimoire/ingest` — but DNS doesn't resolve from sandbox. Need correct URL from Sean.
- Next topics: credible introducer logic, polite professional questioning flow

## Pivot to Own Supabase + Pinata Template
- Guild Grimoire API not ready — sticking with own Supabase for now
- Supabase URL: `https://lrsfpchwdtkokiucbzxl.supabase.co` (creds in `.env.raidguild`)
- Existing tables: `conversations`, `opt_out_users`, `telegram_channels`, `telegram_messages`
- New migration 002: `leads` + `lead_events` tables (NOT YET RUN — needs SQL Editor or service role key)
- `conversations` table gets new `lead_id` FK column to link messages to leads
- Building as a **Pinata agent template** — any org deploys their own lead-agent with a few clicks
- Pinata template docs: https://docs.pinata.cloud/agents/templates/overview
- Created `manifest.json` in repo root
- Created `lead-agent/skills/supabase-crm/SKILL.md` with full PostgREST API patterns
- Updated ARCHITECTURE.md to reflect new direction
- Committed to RaidGuildBDagents repo
